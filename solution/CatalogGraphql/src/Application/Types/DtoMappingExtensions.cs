using Domain;

namespace Application.Types;

public static class DtoMappingExtensions
{
    // public static AuthorDto ToDto(this Author author) =>
    //     new(author.Id, author.FirstName, author.LastName, author.Birthdate, author.OrganizationId);

    // public static BookDto ToDto(this Book book) =>
    //     new(book.Id, book.AuthorId, book.Title, book.DatePublished, book.Price);
}